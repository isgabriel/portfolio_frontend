import { styled } from "styled-components";

const SecondScreenSection = styled.section`
    display: flex;
    flex-direction: column;
    gap: 30px;

    max-width: 980px;

    margin-top: 40px;
`;
const FigurePhotoMe = styled.figure`
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
`;

export { SecondScreenSection, FigurePhotoMe };
